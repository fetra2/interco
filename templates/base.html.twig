<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}

        
        {% block stylesheets %}         
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
            

            <!-- Template Main CSS File -->
            {#<link href="{{asset('css/myapp.css')}}" rel="stylesheet">#}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {#block js nafindra ao am body#}
        
    </head>
    <body> 
        {#     
        <div class="topnav" id="myTopnav">
            <a href="{{path('homepage')}}" class="active">Home</a>
            <a href="{{ path('homepage_show', {slug:'news'}) }}">News</a>
            <a href="#contact">Contact</a>
            <a href="#about">About</a>
            <a href="javascript:void(0);" class="icon" id="responsive_menu">
                <i class="fa fa-bars"></i>
            </a>
            </div>

            <div style="padding-left:16px">
            <h2>Responsive Topnav Example</h2>
            <p>Resize the browser window to see how it works.</p>
        </div>#}
<nav class="navbar navbar-expand-md bg-dark navbar-dark">    
  <a class="navbar-brand" href="{{path('homepage')}}">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="collapsibleNavbar">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link active" href="{{path('homepage')}}"><i class="fa fa-fw fa-home"></i>Acceuil</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{path('homepage_show', {slug:'suivi'})}}"><i class="fa fa-fw fa-search"></i>Suivi</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{path('homepage_show', {slug:'contact'})}}"><i class="fa fa-fw fa-envelope"></i>Contact</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><i class="fa fa-fw fa-user"></i>Log</a>
      </li>    
    </ul>
  </div>  
</nav>
<br>



        {#
        <ul id="navigation">
            {% for key,value in navigation %}
            
                {% if key =='href' %}
                    <li><a {{key}}="{{ value }}">  
                {% else %}
                    {{ value }}
                    </a></li>
                {% endif %}
                
            
            {% endfor %}
        </ul>
        #}
        {% block body %}{% endblock %}

        <footer class="mt-5 p-3 text-center bg-light">
            Made with <i style="color: red;" class="fa fa-heart"></i> by the guys and gals at <a style="color: #444; text-decoration: underline;" href="https://symfonycasts.com">SymfonyCasts</a>
        </footer>

        {% block javascripts %}
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
            <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
            <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
            {{ encore_entry_script_tags('app') }} 
            {#<script src="{{asset('js/myapp.js')}}"></script>  #}
            {#<script src="js/myapp.js"></script> #}
        {% endblock %}
    </body>
</html>
